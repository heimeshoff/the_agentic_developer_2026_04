# CLAUDE.md

Index. Don't put content here — point to files and load them on demand.

## Always available
- `VISION.md` — purpose, ubiquitous language, bounded contexts, scope
- `BACKLOG.md` — In Focus / Backlog / Done

## Load on demand
Files under `memory/` are notes captured over time. Load only the ones relevant to the current task.

- `memory/INDEX.md` — list of memory notes with one-line descriptions

## Conventions
Frontend-only: HTML, CSS, vanilla JS. No frameworks. All code under `source/`. Domain language in identifiers. Full rules in `.claude/skills/worker/SKILL.md`.
